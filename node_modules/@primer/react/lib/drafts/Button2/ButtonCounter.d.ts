import React from 'react';
import { SxProp } from '../../sx';
export type CounterProps = {
    children: number;
} & SxProp;
declare const Counter: ({ children, sx: sxProp, ...props }: CounterProps) => React.JSX.Element;
export { Counter };
